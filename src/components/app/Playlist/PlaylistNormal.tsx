import * as React from "react";
import { Box } from "@material-ui/core";
import { PlaylistProps } from "./playlist";

export const PlaylistNormal: React.FC<PlaylistProps> = props => {
  return <Box></Box>;
};
