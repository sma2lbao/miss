import * as React from "react";
import { MediaNormal } from "@/components/app/Media";

const Test: React.FC = () => {
  return (
    <div style={{ width: 300 }}>
      <MediaNormal loading />
    </div>
  );
};

export default Test;
